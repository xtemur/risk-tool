# Risk Tool Docker Environment Configuration Template
# Copy this to .env.docker and fill in your actual values

# Email Configuration (for sending daily signals)
EMAIL_FROM=your-email@gmail.com
EMAIL_PASSWORD=your-app-password-here

# Email Recipients (comma-separated)
EMAIL_RECIPIENTS=recipient1@example.com,recipient2@example.com

# API Configuration (for data fetching)
API_URL=https://your-api-url.com/api.php
API_USER=your-api-username
API_PASS=your-api-password

# Database Configuration
DB_PATH=/app/data/risk_tool.db

# SMTP Configuration
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587

# Timezone (change to your timezone)
# Examples: UTC, America/New_York, Europe/London, Asia/Tashkent
TZ=UTC
